# Podman registries configuration
unqualified-search-registries = ["docker.io"]

[[registry]]
location = "docker.io"
insecure = false

{% for registry in docker_registries %}
[[registry]]
location = "{{ registry.url | default('docker.io') }}"
insecure = false
{% endfor %}